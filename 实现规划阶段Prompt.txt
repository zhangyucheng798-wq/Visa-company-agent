现在进入“实现规划阶段”。

前置事实：
当前项目已经完成以下阶段，并已通过实现前冻结：
- 项目范围确认
- 头脑风暴收敛
- PM 拆分
- Backend / DBA / Frontend 方案
- Reviewer 总审
- 实现前冻结阶段

重要约束：
1. 所有“是否阻断 = 是”的冻结任务均已达到“已冻结”
2. 当前项目状态已允许进入实现阶段
3. 你不得重新打开已冻结的关键决策，除非发现明确冲突或不可实现问题
4. 你不得回到泛泛产品讨论，也不要重新做大范围头脑风暴
5. 输出必须面向工程执行，能直接转为开发计划、目录结构、任务拆分和实施顺序

请先读取并遵循以下内容：
- AGENTS.md
- docs/PRD.md
- docs/ARCHITECTURE.md
- docs/DATA_MODEL.md
- docs/WORKFLOWS.md
- docs/SECURITY_BASELINE.md
- Reviewer 总审结论
- 全部已冻结产物，尤其包括：
  - 客户访问安全模型
  - 租户隔离执行规范
  - 文件安全控制矩阵
  - 案件主状态机
  - 客户可见状态映射
  - ActionItem / Task 边界
  - 文件域三层对象模型
  - 事件与审计边界
  - 动作型 API 契约
  - 聚合 API 契约
  - 统一枚举与错误码
  - 关键动作审计白名单
  - 审计失败处理矩阵
  - 迁移回滚原则
  - 关键误操作恢复策略
  - 可观测性基线

你的任务：
请把当前方案转成“可执行开发计划”，但先不要直接输出完整代码仓库，也不要直接写业务代码。

输出要求必须包括以下内容：

1. 建议的仓库目录结构
- apps
- packages
- infra
- docs
- tests
- scripts
- shared contracts / types / sdk / ui / rules 的组织方式
- 哪些目录必须先建立
- 哪些目录可以第二阶段再补

2. 第一阶段开发顺序
- 以工程落地和风险优先级排序
- 明确先做哪些基础模块
- 明确为什么这样排序
- 明确哪些实现依赖冻结产物中的哪些结论

3. 每一阶段的交付物
请按阶段列出：
- 阶段目标
- 本阶段范围
- 交付文档
- 交付代码
- 交付测试
- 交付验收结果

4. 每一阶段的依赖项
- 上游冻结产物依赖
- 模块依赖
- 数据库依赖
- 接口依赖
- 前后端协作依赖

5. 每一阶段的测试要求
至少覆盖：
- 单元测试
- 集成测试
- API 契约测试
- 跨租户隔离测试
- 审计与权限测试
- 文件上传/扫描/隔离测试
- 客户访问安全测试
- 关键工作流状态流转测试

6. 每一阶段的回滚考虑
- 数据库迁移回滚
- 状态误迁移修正
- 通知误发恢复
- 文件版本误接受恢复
- 审计失败补偿
- 发布失败回退策略

7. 哪些模块可以并行
8. 哪些模块必须串行
请分别说明原因，不要只列结论。

9. 第一批最值得先落地的接口 / 页面 / 表结构
请明确列出：
- 首批 API
- 首批页面
- 首批核心表
- 首批后台任务 / worker
- 首批审计事件
- 首批错误码 / 枚举
并说明为什么它们是“第一批”。

10. 最小可演示版本定义
请定义一个真正可演示的 MVP Demo，至少说明：
- 用户角色
- 入口页面
- 主流程
- 关键动作
- 成功路径
- 异常路径
- 可演示的审计与安全能力
- 明确哪些属于 Demo 范围，哪些不在本轮实现

11. 工程风险与落地注意事项
- 哪些地方最容易因为理解偏差而返工
- 哪些地方最容易因为冻结产物未落实而出安全问题
- 哪些模块最适合先做脚手架/占位实现
- 哪些模块必须一次做对边界

12. 输出一个“实现阶段执行总表”
表格字段至少包含：
- 编号
- 模块 / 任务项
- 优先级
- 阶段
- 主责角色
- 依赖项
- 是否阻断
- 交付物
- 测试要求
- 风险提示

重要要求：
1. 你必须严格以“已冻结产物”为准，不要自行改动核心边界
2. 如果发现冻结产物之间存在冲突，必须单独列出“实现前冲突清单”，但不要擅自重写全局方案
3. 输出必须是工程执行视角，不要重复泛泛产品分析
4. 输出必须尽量具体，可直接转成项目计划或 issue backlog
5. 默认技术基线遵循当前文档：前端 Next.js + TypeScript，后端 NestJS 或 Fastify，数据库 PostgreSQL，对象存储 S3 兼容，缓存/队列 Redis，要求支持审计、租户隔离、文件安全与可观测性

请按以下结构输出最终结果：
A. 实现阶段总体策略
B. 仓库目录结构
C. 分阶段开发计划
D. 并行 / 串行关系
E. 首批落地清单
F. 测试与回滚要求
G. 最小可演示版本
H. 实现阶段执行总表
I. 实现前冲突清单（如无则明确写“无”）