现在进入“实现前冻结阶段”。

你的角色不是开发者，而是“项目执行总控 + 冻结阶段 PMO + Reviewer 联合把关人”。
除非出现真正无法收敛的阻断决策，否则不要向我反问，直接基于现有文档给出推荐方案并推进冻结。
不要写代码，不要进入实现，不要重新做大范围发散讨论。
你的任务是：严格基于现有文档和冻结看板，推动项目完成实现前的冻结工作，并输出可执行的冻结产物。

请优先读取并遵循以下内容：
- AGENTS.md
- docs/PRD.md
- docs/ARCHITECTURE.md
- docs/DATA_MODEL.md
- docs/WORKFLOWS.md
- docs/SECURITY_BASELINE.md
- Reviewer 总审结论
- 冻结阶段执行看板

全局原则：
1. 不返工大方向，只补齐阻断项和边界项
2. 不脱离现有文档自行扩展核心范围
3. 所有高风险决策必须保留人工介入
4. 不得弱化多租户隔离、客户访问安全、文件安全、审计追踪、回滚恢复
5. 所有冻结项必须形成书面产物，不能只有口头结论
6. 所有“是否阻断 = 是”的任务未达到“已冻结”前，不得进入实现阶段

执行目标：
把项目从“有条件通过”推进到“可进入实现阶段”。

请按以下顺序推进，并一次只处理一个任务，处理完再进入下一个任务：

Phase 1：先处理阻断级安全项
- FZ-01 冻结客户访问安全模型
- FZ-02 冻结租户隔离执行规范
- FZ-03 冻结文件安全控制矩阵

Phase 2：再处理流程与对象骨架
- FZ-04 冻结案件主状态机
- FZ-05 冻结客户可见状态映射
- FZ-06 冻结 ActionItem / Task 边界
- FZ-07 冻结文件域三层对象模型
- FZ-08 冻结事件与审计边界

Phase 3：然后冻结接口
- FZ-09 冻结动作型 API 契约
- FZ-10 冻结聚合 API 契约
- FZ-11 冻结统一枚举与错误码

Phase 4：最后冻结审计与回滚
- FZ-12 冻结关键动作审计白名单
- FZ-13 冻结审计失败处理矩阵
- FZ-14 冻结迁移回滚原则
- FZ-15 冻结关键误操作恢复策略
- FZ-16 冻结可观测性基线

对每一个冻结任务，都必须严格按以下格式输出：

# 任务基本信息
- 编号
- 任务项
- 主责
- 协同
- 是否阻断

# 目标
说明这个冻结项为什么必须先定稿，它解决什么风险，若不冻结会影响什么。

# 输入依据
列出本任务依赖的现有文档、已有结论、相关角色输出。

# 冻结产物
直接产出该任务需要的正式文档内容草案。
文档内容必须可直接进入 docs/ 或项目规范目录，不要只写讨论摘要。

# 决策项
把仍需拍板的问题列成“决策项清单”，每项都要给出：
- 问题
- 可选方案
- 推荐方案
- 推荐理由
- 风险

# 完成标准检查
逐条对照该任务的完成标准，明确：
- 是否满足
- 还缺什么
- 是否可标记为“已冻结”

# Reviewer 复核意见
从 Reviewer 视角补一句结论：
- 通过 / 有条件通过 / 驳回
- 原因
- 是否允许进入下一任务

重要要求：
1. 你必须输出“正式冻结文档草案”，不是泛泛建议
2. 你必须主动发现文档冲突、边界冲突、命名冲突，并在任务内收敛
3. 若当前任务存在未决关键问题，你要明确指出不能冻结，并给出最小决策清单
4. 每完成一个任务后，追加输出当前冻结进度看板，字段统一为：
   - 编号
   - 任务项
   - 当前状态：未开始 / 进行中 / 待评审 / 已冻结 / 阻断
   - 是否阻断：是 / 否
5. 只有当所有“是否阻断 = 是”的任务都达到“已冻结”，才允许你输出“可进入实现阶段”

现在从 FZ-01 开始，不要跳步。
先输出：
1. FZ-01 任务基本信息
2. 《客户访问安全模型说明》草案
3. 《客户资源访问边界表》草案
4. 《Token / Link 生命周期与撤销规则》草案
5. FZ-01 完成标准检查
6. FZ-01 Reviewer 复核意见
7. 当前冻结进度看板